# Politics News Watcher - システム詳細設計書 & 取扱説明書

このドキュメントでは、本システムの仕組み、データの流れ、およびAPIの利用箇所について詳細に解説します。

## 1. システム概要
このシステムは、「特定の政治キーワードに関するニュースを自動収集し、AIが公平な視点で要約して、Discordへ定期配信する」ための自動化ツールです。
ユーザーはGoogleスプレッドシート（またはフォーム）でキーワードと時間を指定するだけで、あとは全自動で稼働します。

## 2. 処理プロセス（データの流れ）

### ① 設定の読み込み (Scheduler)
- **頻度**: 1分ごとにチェック
- **動作**: Googleスプレッドシートを読み込み、「誰が・どのキーワードを・何時に」配信したいかを確認します。

### ② ニュース収集 (Collector)
- **頻度**: 毎時 00分（スケジュール設定）
- **動作**: 
  1. GoogleニュースのRSSフィードにアクセスし、指定キーワードの最新記事URLを取得します。
  2. 取得したURLへアクセスし、記事の本文を抽出します。
  3. **重複チェック**: 過去に収集済みの記事と「意味が似ているか」をAI（ベクトル検索）で判定し、重複していればスキップします。

### ③ データの保存 (Storage)
- **保存先 (優先)**: Googleドライブ (`Politics_News_Watcher_Assets` フォルダ)
- **保存先 (予備)**: ドライブ容量不足時などは、PC内の `assets/` フォルダへ自動保存されます。
- **形式**: テキストファイル (`.txt`) として蓄積され、将来的にNotebookLMなどで分析資料として活用可能です。

### ④ AIによる推敲・清書 (Processor)
- **タイミング**: 指定された配信時間になった瞬間
- **動作**:
  1. **中間レポート作成**: 収集した複数の記事（バラバラの断片）を、AIが「事実」を中心に整理・統合します。
  2. **最終レポート清書**: 「公平・中立なプロの記者」という指示（プロンプト）に基づき、Discordで読みやすい形式に文章を整えます。

### ⑤ 配信 (Discord Bot)
- **動作**: 完成したレポートを、指定されたDiscordのWebhook URLへ送信します。

---

## 3. API利用箇所とコスト（重要）

本システムはいくつかの外部APIを使用しています。運用コストや制限に関わるため、ご認識ください。

### A. Gemini API (Google AI)
- **利用箇所**:
  - 記事のベクトル化（重複判定時）
  - 中間レポート作成
  - 最終レポート清書
- **コスト**: Google AI Studioの無料枠内であれば無料ですが、大量のニュース（数千件/日など）を処理したり、高頻度で実行すると有料枠が必要になる場合があります。

### B. Google Sheets API / Drive API
- **利用箇所**:
  - 設定の読み書き
  - 収集データのドライブ保存
- **コスト**: 基本的に無料ですが、「1分間に**回」といったアクセス数制限があります。本システムは制限にかからないよう、適度な待機時間（sleep）を入れています。

### C. Discord Webhook
- **利用箇所**: レポートの送信
- **コスト**: 完全無料

---

## 4. フォルダ構成の説明

- **`config.py`**: システム全体の設定ファイル（APIキーの読み込みなど）。
- **`scheduler.py`**: システムの心臓部。これを起動しておけば全て動きます。
- **`data/chroma_db`**: 重複判定用のAIデータベース（消すと重複判定がリセットされます）。
- **`assets/`**: ニュース記事のテキストデータ保存場所（ローカル保存時）。

## 5. よくある質問 (FAQ)

**Q. 動画（YouTube）の内容も収集できますか？**
A. 現在の仕様では「テキスト記事（Webニュース）」のみが対象です。動画の内容を理解するには、動画の音声を文字起こしする別の仕組みが必要になるため、現状は非対応です。

**Q. スプレッドシートの行を消してもいいですか？**
A. はい、不要になった設定行は削除して構いません。ただしヘッダー行（1行目）は消さないでください。

**Q. PCの電源を切っても動きますか？**
A. いいえ、このPythonプログラムはご自身のPC上で動いているため、PCがスリープやシャットダウンすると停止します。24時間稼働させたい場合は、クラウドサーバー（VPSなど）への設置が必要です。
