# Politics News Watcher - 運用マニュアル

## セットアップ確認
1. **Googleスプレッドシート**: スプレッドシートに「政治ニュースまとめ（回答）」等の名前で、フォームと連携済みであることを確認してください。
2. **Service Account**: `service_account.json` がフォルダ内にあり、スプレッドシートの「編集者」権限を持っていること。
3. **API Key**: `.env` ファイルに `GEMINI_API_KEY` が設定されていること。

## システムの起動
以下のコマンドでシステムを起動します（常駐モード）。
```powershell
python scheduler.py
```
起動すると、以下のスケジュールで自動処理が走ります。
- **毎時 00分**: 登録された全キーワードのニュース収集実行（Googleドライブへ保存）。
- **毎分**: 配信時間のチェック。指定時間になると、AIがレポートを作成してDiscordへ送信。

## 使い方（キーワードの追加）
1. 作成した **Googleフォーム** から以下の情報を入力してください。
   - 利用者名（例: 自分）
   - キーワード（例: 岸田文雄, 経済 （※カンマ区切りで複数指定可能））
   - 配信希望時間（例: 08:00 (半角)）※必ず HH:MM 形式で入力してください。
   - Discord Webhook URL

2. システムが次のタイミングで自動的に読み込みます。

## トラブルシューティング
- **Discordに届かない**: Webhook URLが正しいか確認してください。また、配信時間が現在時刻と一致しているか確認してください（秒単位はずれるため、分が変わった瞬間にチェックされます）。
- **ニュースが収集されない**: `collector.py` を単体で実行して確認できます。
  ```powershell
  python job_runner.py "キーワード" "ユーザー名"
  ```
- **Geminiエラー**: APIキーの制限を超えている可能性があります。少し待ってから再試行してください。

## 資産（データ）の確認
- Google Driveの **`Politics_News_Watcher_Assets`** フォルダに、年月ごとにテキストファイルが保存されます。
- これらは `processor.py` によって要約の素材として使用されます。
