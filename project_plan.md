# Politics News Watcher - 開発計画書

## 1. プロジェクト概要
**Politics News Watcher** は、特定の政治家やキーワードに関するニュースを自動収集し、AI (Gemini) による推敲・要約を経て、指定された時間にDiscordへ通知するシステムです。
家族など複数人での利用を想定し、GoogleフォームとGoogleスプレッドシートを管理インターフェースとして使用します。また、収集した情報は将来的にNotebookLMなどで活用できるよう、Googleドライブに「資産」として自動蓄積します。

## 2. システム構成

### アーキテクチャ概要
1.  **入力層**: Googleフォーム (スマホ対応)
    *   利用者、キーワード、配信時間、Discord Webhook URL等を登録。
2.  **データ管理層**: Googleスプレッドシート
    *   フォームの回答を蓄積し、システムのマスタDBとして機能。
    *   実行ログ、ドライブ保存先フォルダIDなどを管理。
3.  **処理層**: Pythonアプリケーション (常駐/定期実行)
    *   **Scheduler**: スプレッドシートを監視し、指定時刻にジョブをトリガー。
    *   **Collector**: 1時間ごとにニュースを収集。URLハッシュとベクトル検索で重複を排除。
    *   **Processor**: 6-8時間ごとに中間レポート（仮清書）を作成。配信時刻に最終レポートを作成。
    *   **Asset Manager**: 記事データ、中間レポートをGoogleドライブに構造化して保存。
4.  **出力層**: Discord & Googleドライブ
    *   Discord: ユーザーごとのチャンネルにレポートを通知。
    *   Googleドライブ: 将来の分析・動画生成のためのナレッジベースとしてデータを蓄積。

### 技術スタック
*   **言語**: Python 3.x
*   **AIモデル**: Gemini API (Gemini 2.5 Pro / Flash)
    *   推敲、要約、ベクトル埋め込み (text-embedding-004)
*   **データベース/管理**:
    *   Google Sheets API (`gspread`)
    *   Vector DB (ChromaDB または ローカルFAISS/Lite) - 重複排除用
*   **ストレージ**: Google Drive API
*   **通知**: Discord Webhook
*   **ライブラリ**: `apscheduler`, `feedparser`, `selenium`/`trafilatura` (スクレイピング)

## 3. 機能要件

### A. ニュース収集と重複排除
*   **分散収集**: 1時間ごとに実行。
*   **重複排除**:
    *   Level 1: URLハッシュによる完全一致チェック。
    *   Level 2: Gemini Embeddingsを用いたベクトル類似度チェックで、内容が重複する別記事を排除。

### B. 多段構成によるレポート生成
*   **仮清書 (Intermediate Draft)**:
    *   6-8時間ごとに実行。情報を削ぎ落としすぎず、事実関係とソースを保持した中間データを作成。
*   **最終清書 (Final Report)**:
    *   配信時間直前に実行。中間データを統合し、ユーザー指定のトーンで読みやすいレポートを作成。
*   **コンテキスト管理**:
    *   過去の蓄積データ（キャッシュ）を参照し、情報の鮮度や文脈（以前との違い）を考慮した解説を付与。

### C. 資産化 (Knowledge Base)
*   **フォルダ構造**: `[資産ルート]/[年月]/[議員・キーワード]/`
*   **保存形式**: Markdown (NotebookLM等のソースとして最適化)。
*   **メタデータ**: 記事の重要度、カテゴリ（外交、経済など）をAIがタグ付け。

### D. マルチユーザー・個別スケジュール管理
*   Googleスプレッドシートを利用者、キーワード、時間ごとに管理。
*   API負荷分散のため、実行時間をずらして処理を行うロジックを実装。

## 4. 開発フェーズ

### Phase 1: データ基盤構築
*   プロジェクトフォルダ作成 (`F:\Politics_News_Watcher`)
*   Google Cloud Platform (GCP) プロジェクト設定 (Sheets API, Drive API, Gemini API)。
*   管理用Googleスプレッドシートの設計とAPI接続確認。
*   基本設定ファイル (`config.py`) の作成。

### Phase 2: 収集・保存ロジックの実装
*   ニュース収集スクリプト (`collector.py`) の作成。
*   Googleドライブへの保存機能の実装。
*   ベクトル重複排除ロジックの実装。

### Phase 3: AI推敲・配信ロジックの実装
*   仮清書・最終清書プロンプトの設計。
*   Discord通知機能の実装。
*   スケジュール管理 (`scheduler.py`) の統合。

### Phase 4: テスト・運用
*   シナリオテスト（複数ユーザー、重複記事、時間指定）。
*   長期運用に向けたログ出力設定。
